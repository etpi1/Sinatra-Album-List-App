<h1>All Albums</h1>
<% @songs.each do |songs| %>
  <div class="songs">
  <h3><a href="/songs/<%= songs.id %>"><%= songs.title %></a> <small>(by: <%= songs.author %>)</small></h3>
    <p>
      <%= songs.url %>
    </p>
  </div>
<% end %>

<a href="/songs/new">Post a new song!</a>